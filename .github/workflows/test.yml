name: Test Workflow

on:
  push:
    branches:
      - posts

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "Hello, world!"
